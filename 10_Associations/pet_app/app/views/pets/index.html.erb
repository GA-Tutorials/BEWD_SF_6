<h1><%=@person.name%>'s Pets</h1>

<% @pets.each do |pet| %>
	<li><%= pet.name %> - <%= pet.species %> <%= pet.votes %> <%= link_to 'UPVOTE', vote_path(pet), method: :patch %>
	</li> 

<% end %>

<%= link_to "New Pet", new_person_pet_path(@person) %>